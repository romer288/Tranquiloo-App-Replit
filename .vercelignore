# Exclude scripts (not needed for production)
scripts/**

# Exclude compiled api folder output (Vercel compiles .ts directly)
api/index.js

# Development files
node_modules
.git
.env.local
.env.development

# Build artifacts
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
