# Exclude scripts (not needed for production)
scripts/**

# Exclude server and shared source files (bundled into api/index.js)
server/**
shared/**

# Development files
node_modules
.git
.env.local
.env.development

# Build artifacts
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
